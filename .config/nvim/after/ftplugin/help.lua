-- When opening a help window, move it so that it is a full-height vertical
-- window in the rightmost position (default is to open it as a horizontal
-- window, which is annoying).
vim.cmd.wincmd("L")
